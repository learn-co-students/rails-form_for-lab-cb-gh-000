<%= form_for(@school_class, url: {action: "create"}) do |t| %>
  School Class Form <br>
  <%= t.label :title %>
  <%= t.text_field :title %>
  <%= t.label :room_number %>
  <%= t.number_field :room_number %>
  <%= t.submit %>

<% end %>
